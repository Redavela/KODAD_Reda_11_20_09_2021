import React, { Component } from 'react';

class Apartment extends Component {
    // ici avec react-router-dom récupérer le parametre (id) dans l'url => checker le this
    // une fois récupérer filtrer les apartments pour récupérer celui qui a le bonne id => find
    // afficher les info de l'aprtements

    

    render() { 
        // const {apartment} = this.props;
        // const pathID = this.props.match.params.id
        // function findID(info){
        //     return info.id = pathID
        // }
        // console.log(apartment.find(findID));
        return (
            <div>
                Hello
            </div>
        );
    }
}

export default Apartment;